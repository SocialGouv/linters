{
  "private": true,
  "scripts": {
    "lint": "lerna run lint --parallel"
  },
  "devDependencies": {
    "husky": "^2.6.0",
    "lerna": "^3.15.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "lerna run --concurrency 1 --stream precommit"
    }
  }
}
