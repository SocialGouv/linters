{
  "private": true,
  "scripts": {
    "lint": "lerna run lint --parallel"
  },
  "devDependencies": {
    "husky": "^3.0.1",
    "lerna": "^3.16.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "lerna run --concurrency 1 --stream precommit"
    }
  }
}
